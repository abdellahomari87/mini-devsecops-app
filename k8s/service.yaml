apiVersion: v1
kind: Service
metadata:
  name: mini-devsecops-service

spec:
  type: NodePort

  selector:
    app: mini-devsecops-app

  ports:
    - port: 80
      targetPort: 3000
      nodePort: 30007
